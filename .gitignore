# local test artifacts
ci/logs/
ci/*.log
.DS_Store

# local/private config (host addresses, VM paths, API keys) — copy .env.example to .env
.env

# cohort feature: generated pages contain shared training API keys — never commit
dsh-desktop/cohort/cohorts/*.html
